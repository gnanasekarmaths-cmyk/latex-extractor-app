services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - backend_data:/data
    restart: unless-stopped

volumes:
  backend_data:
